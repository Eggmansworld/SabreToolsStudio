# Normalize line endings: LF in the repository, native on checkout
* text=auto

# Shell scripts must stay LF to run on Linux
*.sh text eol=lf

# PowerShell scripts are Windows-native
*.ps1 text eol=crlf

# Binary assets
*.png binary
*.ico binary
*.jpg binary
